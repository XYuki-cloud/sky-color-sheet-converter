# SkyMusic 参考目录

此目录只用于保存本地的 Sky Music Sheet Maker 参考文件（如果使用者自行放置），帮助理解旧 SkyStudio / 画世界 JSON TXT 格式。统一转换器不依赖其中的可执行文件或源码。

上游项目和许可信息见仓库根目录的 [`THIRD_PARTY_NOTICES.md`](../../../THIRD_PARTY_NOTICES.md)。可执行文件、压缩包和其他外部二进制默认被 `.gitignore` 排除；正式转换请使用 `scripts/` 中的 Python 入口。
